generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model AuthGroup {
  id                   Int                    @id @default(autoincrement())
  name                 String                 @unique @db.VarChar(150)
  authGroupPermissions AuthGroupPermissions[]
  usersUserGroups      UsersUserGroups[]

  @@index([name], map: "auth_group_name_a6ea08ec_like")
  @@map("auth_group")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model AuthGroupPermissions {
  id             BigInt         @id @default(autoincrement())
  groupId        Int            @map("group_id")
  permissionId   Int            @map("permission_id")
  authPermission AuthPermission @relation(fields: [permissionId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissio_permission_id_84c5c92e_fk_auth_perm")
  authGroup      AuthGroup      @relation(fields: [groupId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissions_group_id_b120cbf9_fk_auth_group_id")

  @@unique([groupId, permissionId], map: "auth_group_permissions_group_id_permission_id_0cd325b0_uniq")
  @@index([groupId], map: "auth_group_permissions_group_id_b120cbf9")
  @@index([permissionId], map: "auth_group_permissions_permission_id_84c5c92e")
  @@map("auth_group_permissions")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model AuthPermission {
  id                       Int                        @id @default(autoincrement())
  name                     String                     @db.VarChar(255)
  contentTypeId            Int                        @map("content_type_id")
  codename                 String                     @db.VarChar(100)
  authGroupPermissions     AuthGroupPermissions[]
  djangoContentType        DjangoContentType          @relation(fields: [contentTypeId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_permission_content_type_id_2f476e4b_fk_django_co")
  usersUserUserPermissions UsersUserUserPermissions[]

  @@unique([contentTypeId, codename], map: "auth_permission_content_type_id_codename_01ab375a_uniq")
  @@index([contentTypeId], map: "auth_permission_content_type_id_2f476e4b")
  @@map("auth_permission")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model DjangoAdminLog {
  id                Int                @id @default(autoincrement())
  actionTime        DateTime           @map("action_time") @db.Timestamptz(6)
  objectId          String?            @map("object_id")
  objectRepr        String             @map("object_repr") @db.VarChar(200)
  actionFlag        Int                @map("action_flag") @db.SmallInt
  changeMessage     String             @map("change_message")
  contentTypeId     Int?               @map("content_type_id")
  userId            String             @map("user_id") @db.Uuid
  djangoContentType DjangoContentType? @relation(fields: [contentTypeId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_content_type_id_c4bce8eb_fk_django_co")
  usersUser         UsersUser          @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_user_id_c564eba6_fk_users_user_id")

  @@index([contentTypeId], map: "django_admin_log_content_type_id_c4bce8eb")
  @@index([userId], map: "django_admin_log_user_id_c564eba6")
  @@map("django_admin_log")
}

model DjangoContentType {
  id             Int              @id @default(autoincrement())
  appLabel       String           @map("app_label") @db.VarChar(100)
  model          String           @db.VarChar(100)
  authPermission AuthPermission[]
  djangoAdminLog DjangoAdminLog[]

  @@unique([appLabel, model], map: "django_content_type_app_label_model_76bd3d3b_uniq")
  @@map("django_content_type")
}

model DjangoMigrations {
  id      BigInt   @id @default(autoincrement())
  app     String   @db.VarChar(255)
  name    String   @db.VarChar(255)
  applied DateTime @db.Timestamptz(6)

  @@map("django_migrations")
}

model DjangoSession {
  sessionKey  String   @id @map("session_key") @db.VarChar(40)
  sessionData String   @map("session_data")
  expireDate  DateTime @map("expire_date") @db.Timestamptz(6)

  @@index([expireDate], map: "django_session_expire_date_a5c62663")
  @@index([sessionKey], map: "django_session_session_key_c0390e0f_like")
  @@map("django_session")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model EcommerceTransaction {
  id          String      @id @db.Uuid
  amount      Decimal     @db.Decimal(12, 2)
  currency    String      @db.VarChar(10)
  status      String      @db.VarChar(10)
  reference   String      @unique @db.VarChar(120)
  createdAt   DateTime    @map("created_at") @db.Timestamptz(6)
  userId      String      @map("user_id") @db.Uuid
  vendorId    String      @map("vendor_id") @db.Uuid
  usersUser   UsersUser   @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "ecommerce_transaction_user_id_37835329_fk_users_user_id")
  usersVendor UsersVendor @relation(fields: [vendorId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "ecommerce_transaction_vendor_id_e751e0b6_fk_users_vendor_id")

  @@index([reference], map: "ecommerce_transaction_reference_4be2661a_like")
  @@index([userId], map: "ecommerce_transaction_user_id_37835329")
  @@index([vendorId], map: "ecommerce_transaction_vendor_id_e751e0b6")
  @@map("ecommerce_transaction")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model FinanceBudget {
  id                    BigInt                  @id @default(autoincrement())
  month                 Int
  year                  Int
  totalLimit            Decimal                 @map("total_limit") @db.Decimal(12, 2)
  createdAt             DateTime                @map("created_at") @db.Timestamptz(6)
  userId                String                  @map("user_id") @db.Uuid
  usersUser             UsersUser               @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "finance_budget_user_id_5b42ea65_fk_users_user_id")
  financeBudgetcategory FinanceBudgetcategory[]

  @@index([userId], map: "finance_budget_user_id_5b42ea65")
  @@map("finance_budget")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model FinanceBudgetcategory {
  id            BigInt        @id @default(autoincrement())
  categoryName  String        @map("category_name") @db.VarChar(120)
  limitAmount   Decimal       @map("limit_amount") @db.Decimal(12, 2)
  budgetId      BigInt        @map("budget_id")
  financeBudget FinanceBudget @relation(fields: [budgetId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "finance_budgetcategory_budget_id_dbbf6bcc_fk_finance_budget_id")

  @@index([budgetId], map: "finance_budgetcategory_budget_id_dbbf6bcc")
  @@map("finance_budgetcategory")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model FinanceExpense {
  id            BigInt       @id @default(autoincrement())
  category      String       @db.VarChar(120)
  amount        Decimal      @db.Decimal(12, 2)
  paymentMethod String?      @map("payment_method") @db.VarChar(50)
  date          DateTime     @db.Date
  note          String?
  itemId        BigInt?      @map("item_id")
  userId        String       @map("user_id") @db.Uuid
  vendorId      String?      @map("vendor_id") @db.Uuid
  marketItem    MarketItem?  @relation(fields: [itemId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "finance_expense_item_id_710e4dce_fk_market_item_id")
  usersUser     UsersUser    @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "finance_expense_user_id_077c4557_fk_users_user_id")
  usersVendor   UsersVendor? @relation(fields: [vendorId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "finance_expense_vendor_id_bef4c853_fk_users_vendor_id")

  @@index([itemId], map: "finance_expense_item_id_710e4dce")
  @@index([userId], map: "finance_expense_user_id_077c4557")
  @@index([vendorId], map: "finance_expense_vendor_id_bef4c853")
  @@map("finance_expense")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model MarketForecast {
  id             BigInt     @id @default(autoincrement())
  forecastDate   DateTime   @map("forecast_date") @db.Date
  predictedPrice Decimal    @map("predicted_price") @db.Decimal(12, 2)
  modelUsed      String     @map("model_used") @db.VarChar(50)
  confidenceLow  Decimal?   @map("confidence_low") @db.Decimal(12, 2)
  confidenceHigh Decimal?   @map("confidence_high") @db.Decimal(12, 2)
  generatedAt    DateTime   @map("generated_at") @db.Timestamptz(6)
  itemId         BigInt     @map("item_id")
  marketItem     MarketItem @relation(fields: [itemId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "market_forecast_item_id_cac28c88_fk_market_item_id")

  @@index([itemId], map: "market_forecast_item_id_cac28c88")
  @@map("market_forecast")
}

model MarketItem {
  id                    BigInt                  @id @default(autoincrement())
  name                  String                  @unique @db.VarChar(120)
  category              String                  @db.VarChar(100)
  unit                  String                  @db.VarChar(30)
  financeExpense        FinanceExpense[]
  marketForecast        MarketForecast[]
  marketNationalprice   MarketNationalprice[]
  marketPricesubmission MarketPricesubmission[]
  marketVendorprice     MarketVendorprice[]

  @@index([name], map: "market_item_name_733d40ed_like")
  @@map("market_item")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model MarketNationalprice {
  id         BigInt     @id @default(autoincrement())
  price      Decimal    @db.Decimal(10, 2)
  source     String     @db.VarChar(20)
  city       String     @db.VarChar(120)
  date       DateTime   @db.Date
  itemId     BigInt     @map("item_id")
  marketItem MarketItem @relation(fields: [itemId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "market_nationalprice_item_id_8dae6c55_fk_market_item_id")

  @@index([itemId], map: "market_nationalprice_item_id_8dae6c55")
  @@map("market_nationalprice")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model MarketPricesubmission {
  id             BigInt     @id @default(autoincrement())
  priceValue     Decimal    @map("price_value") @db.Decimal(10, 2)
  marketLocation String     @map("market_location") @db.VarChar(120)
  city           String     @db.VarChar(120)
  dateObserved   DateTime   @map("date_observed") @db.Date
  status         String     @db.VarChar(20)
  createdAt      DateTime   @map("created_at") @db.Timestamptz(6)
  itemId         BigInt     @map("item_id")
  userId         String     @map("user_id") @db.Uuid
  marketItem     MarketItem @relation(fields: [itemId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "market_pricesubmission_item_id_4049dcf2_fk_market_item_id")
  usersUser      UsersUser  @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "market_pricesubmission_user_id_9826e558_fk_users_user_id")

  @@index([itemId], map: "market_pricesubmission_item_id_4049dcf2")
  @@index([userId], map: "market_pricesubmission_user_id_9826e558")
  @@map("market_pricesubmission")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model MarketVendorprice {
  id          BigInt      @id @default(autoincrement())
  price       Decimal     @db.Decimal(10, 2)
  date        DateTime    @db.Date
  isVerified  Boolean     @map("is_verified")
  itemId      BigInt      @map("item_id")
  vendorId    String      @map("vendor_id") @db.Uuid
  marketItem  MarketItem  @relation(fields: [itemId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "market_vendorprice_item_id_22265e99_fk_market_item_id")
  usersVendor UsersVendor @relation(fields: [vendorId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "market_vendorprice_vendor_id_c8f441f1_fk_users_vendor_id")

  @@index([itemId], map: "market_vendorprice_item_id_22265e99")
  @@index([vendorId], map: "market_vendorprice_vendor_id_c8f441f1")
  @@map("market_vendorprice")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model UsersNotification {
  id        BigInt    @id @default(autoincrement())
  type      String    @db.VarChar(50)
  message   String
  isRead    Boolean   @map("is_read")
  createdAt DateTime  @map("created_at") @db.Timestamptz(6)
  userId    String    @map("user_id") @db.Uuid
  usersUser UsersUser @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_notification_user_id_fed360c8_fk_users_user_id")

  @@index([userId], map: "users_notification_user_id_fed360c8")
  @@map("users_notification")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model UsersUser {
  password                 String                     @db.VarChar(128)
  lastLogin                DateTime?                  @map("last_login") @db.Timestamptz(6)
  isSuperuser              Boolean                    @map("is_superuser")
  id                       String                     @id @db.Uuid
  fullName                 String                     @map("full_name") @db.VarChar(120)
  email                    String                     @unique @db.VarChar(254)
  phone                    String?                    @unique @db.VarChar(20)
  role                     String                     @db.VarChar(10)
  city                     String?                    @db.VarChar(100)
  householdSize            Int?                       @map("household_size")
  incomeBracket            String?                    @map("income_bracket") @db.VarChar(50)
  isStaff                  Boolean                    @map("is_staff")
  isActive                 Boolean                    @map("is_active")
  createdAt                DateTime                   @map("created_at") @db.Timestamptz(6)
  notificationPreferences  Json                       @map("notification_preferences")
  onboardingCompleted      Boolean                    @map("onboarding_completed")
  djangoAdminLog           DjangoAdminLog[]
  ecommerceTransaction     EcommerceTransaction[]
  financeBudget            FinanceBudget[]
  financeExpense           FinanceExpense[]
  marketPricesubmission    MarketPricesubmission[]
  usersAuditlog            UsersAuditlog[]
  usersNotification        UsersNotification[]
  usersSystemsetting       UsersSystemsetting[]
  usersUserGroups          UsersUserGroups[]
  usersUserUserPermissions UsersUserUserPermissions[]
  usersVendor              UsersVendor[]

  @@index([email], map: "users_user_email_243f6e77_like")
  @@index([phone], map: "users_user_phone_fe37f55c_like")
  @@map("users_user")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model UsersUserGroups {
  id        BigInt    @id @default(autoincrement())
  userId    String    @map("user_id") @db.Uuid
  groupId   Int       @map("group_id")
  authGroup AuthGroup @relation(fields: [groupId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_user_groups_group_id_9afc8d0e_fk_auth_group_id")
  usersUser UsersUser @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_user_groups_user_id_5f6f5a90_fk_users_user_id")

  @@unique([userId, groupId], map: "users_user_groups_user_id_group_id_b88eab82_uniq")
  @@index([groupId], map: "users_user_groups_group_id_9afc8d0e")
  @@index([userId], map: "users_user_groups_user_id_5f6f5a90")
  @@map("users_user_groups")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model UsersUserUserPermissions {
  id             BigInt         @id @default(autoincrement())
  userId         String         @map("user_id") @db.Uuid
  permissionId   Int            @map("permission_id")
  authPermission AuthPermission @relation(fields: [permissionId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_user_user_perm_permission_id_0b93982e_fk_auth_perm")
  usersUser      UsersUser      @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_user_user_permissions_user_id_20aca447_fk_users_user_id")

  @@unique([userId, permissionId], map: "users_user_user_permissions_user_id_permission_id_43338c45_uniq")
  @@index([permissionId], map: "users_user_user_permissions_permission_id_0b93982e")
  @@index([userId], map: "users_user_user_permissions_user_id_20aca447")
  @@map("users_user_user_permissions")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model UsersVendor {
  id                   String                 @id @db.Uuid
  shopName             String                 @map("shop_name") @db.VarChar(150)
  city                 String                 @db.VarChar(120)
  latitude             Decimal?               @db.Decimal(10, 6)
  longitude            Decimal?               @db.Decimal(10, 6)
  joinedAt             DateTime               @map("joined_at") @db.Timestamptz(6)
  ownerId              String                 @map("owner_id") @db.Uuid
  address              String                 @db.VarChar(255)
  contactPhone         String                 @map("contact_phone") @db.VarChar(30)
  isVerified           Boolean                @map("is_verified")
  ratingAvg            Decimal                @map("rating_avg") @db.Decimal(3, 2)
  ratingCount          Int                    @map("rating_count")
  ecommerceTransaction EcommerceTransaction[]
  financeExpense       FinanceExpense[]
  marketVendorprice    MarketVendorprice[]
  usersUser            UsersUser              @relation(fields: [ownerId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_vendor_owner_id_5f619c62_fk_users_user_id")

  @@index([ownerId], map: "users_vendor_owner_id_5f619c62")
  @@map("users_vendor")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model UsersAuditlog {
  id         BigInt     @id @default(autoincrement())
  action     String     @db.VarChar(120)
  resource   String     @db.VarChar(120)
  resourceId String     @map("resource_id") @db.VarChar(120)
  detail     Json
  createdAt  DateTime   @map("created_at") @db.Timestamptz(6)
  actorId    String?    @map("actor_id") @db.Uuid
  usersUser  UsersUser? @relation(fields: [actorId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_auditlog_actor_id_c75b00f8_fk_users_user_id")

  @@index([actorId], map: "users_auditlog_actor_id_c75b00f8")
  @@map("users_auditlog")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model UsersSystemsetting {
  id          BigInt     @id @default(autoincrement())
  key         String     @unique @db.VarChar(120)
  value       Json
  updatedAt   DateTime   @map("updated_at") @db.Timestamptz(6)
  updatedById String?    @map("updated_by_id") @db.Uuid
  usersUser   UsersUser? @relation(fields: [updatedById], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_systemsetting_updated_by_id_289e090c_fk_users_user_id")

  @@index([key], map: "users_systemsetting_key_853d6952_like")
  @@index([updatedById], map: "users_systemsetting_updated_by_id_289e090c")
  @@map("users_systemsetting")
}
